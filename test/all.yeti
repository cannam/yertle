
program test.all;

{ runTests } = load test.test;

tests = [
    "hashTree" : load test.test_hashtree,
    "index" : load test.test_index,
];

bad = sum (mapHash do name testHash: runTests name testHash done tests);

if (bad > 0) then
    println "\n** \(bad) test(s) failed!";
else
    ()
fi

